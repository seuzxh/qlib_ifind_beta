# 为待修改文件添加审查注释

在 5 个文件中添加中文注释，标注审查发现的 6 个问题点。注释格式统一用 `# ⚠️ 审查: ` 前缀，方便后续 grep 定位。

## 文件 1: `qlib_ifind_beta/live/inference.py`

**问题**：rolling retrain 断链 + use_online 默认 False

- 第 50 行 `use_online: bool = False`：
  添加注释说明当前 FROZEN 为默认、rolling 未接入、计划改为 True
- 第 69-82 行 model 加载分支：
  添加注释说明 use_online 分支虽已实现但 live_forward 从未传入，是死代码

## 文件 2: `qlib_ifind_beta/realtime/signal.py`

**问题**：ZScoreNorm 死代码 + universe 不自动刷新 + 因子重算 + 模型硬编码

- 第 1-14 行 module docstring：
  添加中文说明（当前是英文），注明与 predict_day 的关系
- 第 61-98 行 `_compute_all_factors`：
  标注 4 个 extra 因子（vol_vs_yest_t2/t3/t5 + overnight_gap）与 materialize_minute.py 的公式应共享但目前手算
- 第 112-116 行 `load_universe(target_date)`：
  标注 universe 可能返回空（T 日未刷新），需要自动刷新
- 第 229-233 行 ZScoreNorm 查找：
  标注这是死代码（handler infer_processors=[] 永远返回 None）
- 第 253-273 行手动 ProcessInf→ZScoreNorm→Fillna：
  标注 ZScoreNorm 分支永远不执行，实际只执行 fillna(0)
- 第 283-285 行 model 加载：
  标注硬编码 CHAMPION_RECORDER_ID，未接 rolling use_online 路径

## 文件 3: `scripts/retrain.py`

**问题**：只跑过一次从未滚动前进

- 第 1-21 行 module docstring：
  添加注释说明实际状态（只跑过 1 次 test=2026-04-01，从未 gen_following_tasks）
- 第 132-152 行首次 vs 后续分支：
  标注首次分支跑过但后续分支从未执行

## 文件 4: `scripts/live_forward.py`

**问题**：predict_day 未传 use_online

- 第 86 行 `result = predict_day(date)`：
  标注未传 use_online=True，始终用 FROZEN champion

## 文件 5: `qlib_ifind_beta/realtime/data_fetch.py`

**问题**：load_universe 返回空 + factor fallback

- 第 319-350 行 `load_universe`：
  标注 T 日 universe 可能空（需上游刷新），realtime 路径调用方需处理空返回
- 第 258-264 行 kline-fetcher fallback：
  已有 NOTE 注释，补充中文说明为何不能用日频 volume 替代

## 不改动的文件

- `qlib_ifind_beta/minute_factors.py`：compute_day_factors 已共享，无需标注
- `qlib_ifind_beta/materialize_minute.py`：公式正确，只需在 vol_vs_yest 族标注与 signal.py 的共享计划

## 验证

注释添加后，`grep -rn "⚠️ 审查" qlib_ifind_beta/ scripts/` 能定位所有待修改点。
