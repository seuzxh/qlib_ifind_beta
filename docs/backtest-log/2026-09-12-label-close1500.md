---
layout: default
title: label 改用 15:00 分钟收盘（close1500）验证——不切换
parent: "验证与研究"
nav_order: 8
---

# label 改用 15:00 分钟收盘（close1500）验证——不切换

> 日期：2026-09-12  
> 状态：**验证未通过（滚动 21%/37% 胜率）；但 2026-09-12 用户决策采纳纯分钟版
> 为后续基线**——验证记录原样保留，决策依据与生效边界见下方「采纳记录」  
> 变体：`Ref($close1500, -1) / $price_941 - 1`（两腿同出 1min 序列，内部一致，
> 消除混合基准噪声）vs 现役 `Ref($close, -1) / $price_941 - 1`  
> 因子、模型、策略、成本全部冻结，单变量= label；产物 `data/label1500_*.csv`、
> 实验 `label1500_*`；研究 bin `close1500.day.bin`

## 结果

| 层 | 现役 label | close1500 label |
|---|---|---|
| W1 PortAna（超额/IR/DD） | +39.2% / 3.65 / -14.8% | **+45.1% / 4.75 / -10.6%** ✅ |
| W2 PortAna | +34.1% / 3.83 / -14.1% | **+11.4% / 1.28** ❌ |
| purged rolling 19 段（卖出=日频 close） | 基线 | **胜率 21%，-3.18%/段** ❌ |
| purged rolling 19 段（卖出=close1500，训练/执行全一致） | — | 胜率 21%，-3.71%/段（与上一行几乎相同，仅 1 段不同） |
| **纯分钟版**（close1500/c941raw，09:41 腿去 V4 校正） | +39.2% | W1 +49.2%（IR 5.03，全日最高）/ W2 +19.5%；**purged 胜率 37%，-3.82%/段** ❌ |

## 纯分钟版补充（同日）

两腿全部取 1min 序列原值（新增研究 bin `c941raw`，去掉 ~3% 异常票的 V4 日频
校正）：W1 +49.2%/IR 5.03（全日所有变体最高）、W2 +19.5%、purged 胜率 37%
（-3.82%/段）。与 V4 版同样呈现"W1 改善、W2 崩、滚动否"的格局，且两版差异
（V4 与非 V4 的 09:41 腿）只影响幅度不影响方向——**分钟 label 家族两种构造
均未通过，结论不变：不切换**。

## 结论

1. **不切换**。W1 的改善是单窗运气；W2 崩塌 + 19 段滚动 21% 胜率双重否定。
   把卖出价也换成 close1500（消除训练/执行口径错配）几乎不改变结果——
   差异不在成交价实现，而在模型选股质量本身。
2. **机理收尾**：现役 label 的"混合基准噪声"并非纯噪声——它是两套数据序列
   对同一真实时刻（T+1 收盘）的**系统性分歧**，可被模型学习且样本外持续存在，
   而回测结算恰恰用日频 close bin。在回测语义下，现役 label 是该交易的忠实
   定义；close1500 是"更干净但描述了另一笔结算"的代理。
3. **真正的修复仍是上游**：factor 阶梯化重建让两套序列收敛到同一真实价格后，
   两个 label 应趋同，届时再重估。在此之前任何单侧"清洗"都会丢掉模型正在
   利用的结构（与 gap 口径系列实验同构的第六次验证）。

## 采纳记录（2026-09-12，本日志收尾）

用户在审阅全部验证证据后决策：**采纳纯分钟版 label 为后续基线**，覆盖本日志的
保守建议。生效边界：

- **表达式**：`Ref($close1500, -1) / $close0941 - 1`（`CHAMPION_LABEL_EXPR`、
  champion yaml 同步；字段重命名 c941raw→close0941，与 bin 逐位等值）；
- **生产化**：`materialize_minute.py` 现写出 22 bin/股（含 close0941/close1500 两腿），
  build_overlay 全链路可复现；测试 118 项全过；
- **不受影响**：冻结 Champion（93d435e0）的生产评分——label 不进推理；影子回放
  零漂移门禁（比对 pred，与 label 无关）；deal_price 仍为 `[$price_941, $close]`
  （成交合同未动，训练口径与执行口径存在已知差异，如实记录）；
- **受影响**：一切后续训练（retrain/rolling/compare/inference 的 label 结构）；
- **决策考量（用户视角）**：口径一致性优先 + 为上游 factor 重建后的世界对齐；
  W2/滚动劣势的验证事实如上表保留，不因决策改写。

## 环境备注

研究 bin `close1500.day.bin` 已入 overlay（additive）；评估阶段现役 label/yaml 未动，
采纳后已正式切换（见上节）。c941raw 已重命名 close0941 并由物化管线接管。ManualBT 已支持
`sell_field` 参数，与 `buy_field` 一起构成延迟执行/口径对照工具组。
